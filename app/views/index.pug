doctype html
html.html
  head
    title App

    meta(charset='utf-8')

    script.
      window.__INITIAL_STATE__ = !{JSON.stringify(initialState || {})};

  body.body
    != ssr

    div#root!= markup

